jacoco {
    toolVersion = "0.8.8"
}

// TODO Add JaCoCo reporting to PRs: https://github.com/marketplace/actions/jacoco-report
jacocoTestReport {
    dependsOn test // tests are required to run before generating the report
    reports {
        xml.required = true
    }
    afterEvaluate { // (optional) : to exclude classes / packages from coverage
        classDirectories.from = files(classDirectories.files.collect {
            fileTree(
                    dir: it,
                    exclude: [
                        'com/slalombuild/movieman/MoviemanApplication.class',
                        'com/slalombuild/movieman/config/**',
                        'com/slalombuild/movieman/domain/entity/**',\
                        'com/slalombuild/movieman/domain/repository/**',
                        'com/slalombuild/movieman/usecase/model/**'
                    ]
            )
        })
    }
}
